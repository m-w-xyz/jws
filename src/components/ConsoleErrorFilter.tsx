"use client";

/**
 * Mounted from root layout — reserved for dev-only console noise suppression.
 * Must export a default component so the module type-checks under Next/React.
 */
export default function ConsoleErrorFilter() {
  return null;
}
